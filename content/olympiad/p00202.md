+++
date = "2024-06-26"
draft = false
title = "Problem 00202"
code = "p00202"
source = "Concursul Interjudetean de Matematica \"UNIREA\", Ediția 2014, Focsani, Romania"
problemTags = ["algebra", "logarithms", "equations", "inequalities"]
difficulty = "medium"
gradeLevel = ["10th"]
usekatex = true
origin = "Logarithmic Challenges from Romanian Math Olympiads"
+++

<div class="mp" id="p00202">
<p><a class="mpl" href="/olympiad/p00202/">Problem 00202</a></p>
<p>Determine the real numbers \(x \in (\log_3 2, +\infty)\) with the property that:</p>
<p class="mpc">
    \[
        \log_2 \left( 3^{\log_2(3^x - 1)} - 1 \right) = \log_3 \left( 2^{\log_3(2^x + 1)} + 1 \right)
    \]
</p>
<details>
    <summary>Hint</summary>
    <p>Define \(g(t)=\log_2(3^t-1)\), identify \(g^{-1}\), and use the fact that both functions are strictly increasing.</p>
</details>
<details>
    <summary>Solution</summary>
    <p>Define the strictly increasing bijection \(g:(0,\infty)\to\mathbb R\) by \(g(t)=\log_2(3^t-1)\). Its inverse is found by setting \(y=\log_2(3^t-1)\) and solving for \(t\):</p>
    <p class="mpc">
        \[ 2^y = 3^t - 1 \implies 3^t = 2^y + 1 \implies t = \log_3(2^y + 1) \]
    </p>
    <p>Thus \(g^{-1}(t)=\log_3(2^t+1)\). Because the given \(x>\log_3 2\), we have \(g(x)>0\), so all the following compositions are defined. The original equation becomes:</p>
    <p class="mpc">
        \[ g(g(x)) = g^{-1}(g^{-1}(x)) \]
    </p>
    <p>We now justify the key reduction. If \(g(x)>g^{-1}(x)\), applying the increasing functions \(g\) and \(g^{-1}\), respectively, gives</p>
    <p class="mpc">
        \[
            g(g(x))>g(g^{-1}(x))=x
            >g^{-1}(g^{-1}(x)),
        \]
    </p>
    <p>contradicting the equation. The reverse inequality leads to the reverse contradiction. Hence \(g(x)=g^{-1}(x)\).</p>
    <p>Let \(\log_2(3^x - 1) = a\), which means \(g(x) = a\). Since we know \(g(x) = g^{-1}(x)\), we also have \(g^{-1}(x) = a\). This gives us the system:</p>
    <p class="mpc">
        \[
        \begin{cases}
            3^x - 1 = 2^a \\
            2^x + 1 = 3^a
        \end{cases}
        \]
    </p>
    <p>Adding these two equations together yields:</p>
    <p class="mpc">
        \[ 3^x + 2^x = 3^a + 2^a \]
    </p>
    <p>Because the function \(h(t) = 3^t + 2^t\) is strictly increasing, it must be that \(x = a\). Substituting this back into our first equation, we get:</p>
    <p class="mpc">
        \[ 3^x - 1 = 2^x \implies 3^x - 2^x = 1 \]
    </p>
    <p>Dividing by \(3^x\), the equation becomes \(1 - \left(\frac{2}{3}\right)^x = \left(\frac{1}{3}\right)^x\). Since the left side is strictly increasing and the right side is strictly decreasing, there is only one possible solution:</p>
    <p class="mpc">
        \[ x = 1 \]
    </p>
</details>
<details>
    <summary>Source</summary>
    <p>Concursul Interjudetean de Matematica "UNIREA", Ediția 2014, Focsani, Romania</p>
</details>
</div>
